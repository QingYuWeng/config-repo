spring:
  application:
    name: client

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone:  http://localhost:8761/eureka/,http://localhost:8762/eureka/,http://localhost:8763/eureka/
  instance:
#    服务中心现实显示的名字
    hostname: client
